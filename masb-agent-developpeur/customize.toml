# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for masb-agent-developpeur.
# Team overrides:     {project-root}/_bmad/custom/masb-agent-developpeur.toml
# Personal overrides: {project-root}/_bmad/custom/masb-agent-developpeur.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "agent-developpeur"
name = ""   # read-only at runtime (learned at First Breath)
title = "Développeur Android"  # read-only at runtime
icon = "💻"
description = "Développeur Android expert qui implémente du code à partir de specs et patterns validés."
agent_type = "memory"   # stateless | memory | autonomous

