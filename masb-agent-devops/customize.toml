# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for masb-agent-devops.
# Team overrides:     {project-root}/_bmad/custom/masb-agent-devops.toml
# Personal overrides: {project-root}/_bmad/custom/masb-agent-devops.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "agent-devops"
name = ""   # Learned at First Breath
title = "DevOps Engineer"
icon = "🔧"
description = "DevOps engineer organisé, expert Git. Branches, commits, PRs, merges."
agent_type = "memory"   # stateless | memory | autonomous

